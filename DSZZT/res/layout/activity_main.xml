<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/automat"
        android:onClick="onClick"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/manual"
        android:layout_centerHorizontal="true"
        android:height="80dp"
        android:text="Tryb automatyczny"
        android:width="170dp" />

    <Button
        android:id="@+id/manual"
        android:onClick="onClick"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/automat"
        android:layout_alignParentTop="true"
        android:layout_marginTop="102dp"
        android:height="80dp"
        android:text="Tryb manualny"
        android:width="170dp" />

    <Button
        android:id="@+id/videostream"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/automat"
        android:layout_below="@+id/automat"
        android:height="80dp"
        android:onClick="onClick"
        android:text="VideoStreaming"
        android:width="170dp" />

</RelativeLayout>
